import React from 'react';
import { Grid, Paper, Typography } from '@material-ui/core';

const VideoItem = ({ videoitem }) => {
  return <div>{console.log(videoitem)}</div>;
};

export default VideoItem;
